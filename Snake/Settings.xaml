<Window x:Class="Snake.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Snake"
        mc:Ignorable="d"
        Title="Settings" Height="180" Width="400"
        MinHeight="180" MinWidth="300"
        Background="{StaticResource BackgroundColor}"
        Foreground="{StaticResource TextColor}"
        FontFamily="{StaticResource MainFont}"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
       
        >
    <Border BorderThickness="5" CornerRadius="5" BorderBrush="AliceBlue" >
        <Border BorderThickness="2" CornerRadius="2" BorderBrush="Teal" >
            <StackPanel Margin="10">
                <TextBlock Text="Height [15,25]" Margin="5 5 5 5"/>
                <Slider x:Name="HeightSlider" Maximum="25" Minimum="15" TickFrequency="1" TickPlacement="BottomRight" Value="15" Margin="0 0 5 5"/>

                <TextBlock Text="Width [15,25]" Margin="5 5 5 5"/>
                <Slider x:Name="WidthSlider" Maximum="25" Minimum="15" TickFrequency="1" TickPlacement="BottomRight" Value="15" Margin="0 0 5 5"/>

                <Button Content="SAVE" Click="SaveButton_Click" Width="50" Margin="10 10 10 10"/>
            </StackPanel>
        </Border>
    </Border>
</Window>
